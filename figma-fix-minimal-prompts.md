# ManLog Figma 最小修复 Prompt（针对现有稿）

> 用途：不推翻重做，只修当前 Figma 已生成页面中的关键问题。  
> 使用方式：按顺序复制到 Figma AI，先跑“全局修复”，再按页面局部修复。

---

## 1) 全局修复（先执行）

```text
请基于我当前已有的微信小程序设计稿做“最小改动修复”，不要重做整体风格和布局，仅修以下问题：

1) 术语统一替换（全页面）：
- 遗精 -> 生理现象
- 性生活 -> 亲密互动
- 自慰 -> 自我舒缓

2) 信息架构：
- 底部Tab固定为：记录 / 统计 / 我的

3) 登录策略：
- 登录不是首屏强制页
- 用户触发以下操作时才弹出登录引导弹窗：
  - 点击新增记录
  - 点击编辑记录
  - 点击删除记录
  - 查看我的历史记录
  - 查看我的真实统计

4) 登录引导弹窗：
- 保留“微信一键登录”主按钮
- 移除“先看看（仅示例页）”按钮

请保持原有视觉风格、颜色和组件风格不变，只修改上述内容。
```

---

## 2) 统计页修复（重点）

```text
请仅修复统计页，不改其他页面。严格按以下规则：

A. 维度切换：
- 周 / 月 / 年 / 总

B. 柱状图规则（必须）：
- 横坐标=时间维度，纵坐标=聚合次数count
- 周：显示当前周周一到周日每一天（MM.DD）
- 月：显示当前月 01 到月末每天
- 年：显示当前年 01月 到 12月
- 总：显示各年份（YYYY）
- 禁止把类型放到横坐标
- 单柱最大视觉宽度建议 28px，避免柱子过宽

C. 频率趋势折线图规则（必须）：
- 周：最近8周（Wxx）
- 月：最近12个月（YYYY-MM）
- 年：最近5年（YYYY）
- 总：全部年份（YYYY）
- 纵坐标为对应周期聚合次数
- 不要与柱状图使用完全相同横坐标

D. 指标卡规则（必须）：
- 周均次数 / 月均次数
  - 周视图：周均=本周总次数，月均显示“-”
  - 月视图：周均=当月总次数/当月周数，月均=当月总次数
  - 年视图：周均=当年总次数/当年周数，月均=当年总次数/12
  - 总视图：周均=累计总次数/累计周数，月均=累计总次数/累计月数
- 高频时间段必须显示“时间段”，例如 18:00-23:59
- 禁止显示成单一时间点（如 21:00）

请在不改变整体视觉风格的前提下完成修复。
```

---

## 3) 记录页修复（时间逻辑）

```text
请仅修复记录页（新增/编辑），不改整体布局：

1) 类型筛选和类型标签统一为：
- 生理现象 / 亲密互动 / 自我舒缓

2) 时间字段交互说明（页面文案或交互标注中体现）：
- 新增态：时间默认显示当前时间（可修改）
- 编辑态：时间回显该条记录已保存时间（可修改）

3) 未登录点击“新增”时，展示登录引导弹窗（流程标注即可）。
```

---

## 4) 快速验收清单（生成后自检）

- [ ] 所有旧术语已替换
- [ ] 底部 Tab 为“记录 / 统计 / 我的”
- [ ] 登录不是首屏强制，且关键操作会唤起登录弹窗
- [ ] 登录弹窗已移除“先看看（仅示例页）”
- [ ] 统计页新增“总”维度
- [ ] 柱状图横轴是时间维度，纵轴是聚合次数
- [ ] 趋势图与柱状图口径不重复
- [ ] 高频时间段显示为时间段而非时间点
