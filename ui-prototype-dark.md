# ManLog 深色主题（暗黑模式）UI 规范（v1）

> 用途：为 `ui-prototype.md` 提供一套可直接落地的 Dark Theme 方案。  
> 目标：在低光环境下提供更舒适、更私密、更高级的视觉体验，并保持图表可读性。

## 1. 主题设计原则

- 私密优先：降低高亮噪声，避免过度鲜艳。
- 对比可读：文本与背景对比达到可读标准（正文建议 >= 4.5:1）。
- 一致映射：浅色主题与深色主题的语义色保持一致（成功/警告/危险不变语义）。
- 图表清晰：深色背景下保证柱状图/折线图/饼图层级明确。

---

## 2. Dark Theme 设计 Tokens

## 2.1 基础颜色

- 背景主色 `Dark-Bg-Page`: `#0B1020`
- 背景次色 `Dark-Bg-Surface`: `#11182B`
- 卡片背景 `Dark-Bg-Card`: `#131C31`
- 浮层背景 `Dark-Bg-Overlay`: `#0F172A`
- 分割线 `Dark-Border`: `#263248`

- 主文字 `Dark-Text-Primary`: `#E5E7EB`
- 次文字 `Dark-Text-Secondary`: `#9CA3AF`
- 弱文字 `Dark-Text-Tertiary`: `#6B7280`
- 占位文字 `Dark-Text-Placeholder`: `#64748B`

## 2.2 品牌与功能色（深色适配）

- 主色 `Dark-Primary`: `#818CF8`
- 主色高亮 `Dark-Primary-Emphasis`: `#A5B4FC`
- 主色浅底 `Dark-Primary-Soft`: `rgba(129,140,248,0.18)`

- 成功 `Dark-Success`: `#22C55E`
- 警示 `Dark-Warning`: `#F59E0B`
- 危险 `Dark-Danger`: `#F87171`
- 信息 `Dark-Info`: `#38BDF8`

## 2.3 图表专用色（深色）

- `Chart-Physio`: `#60A5FA`（生理现象）
- `Chart-Intimacy`: `#34D399`（亲密互动）
- `Chart-Relief`: `#C084FC`（自我舒缓）
- 网格线 `Chart-Grid`: `rgba(148,163,184,0.22)`
- 坐标轴文字 `Chart-Label`: `#94A3B8`

## 2.4 阴影与描边

- 卡片阴影：`0 6 20 rgba(2,6,23,0.45)`
- 弹窗阴影：`0 12 36 rgba(2,6,23,0.55)`
- 卡片描边：`1px solid rgba(148,163,184,0.14)`

## 2.5 字体与间距（继承浅色版）

- 字号、字重、栅格间距沿用 `ui-prototype.md`
- 仅调整颜色、阴影、状态色和图表色映射

---

## 3. 组件暗色规范

## 3.1 按钮

- 主按钮：
  - 背景：`Dark-Primary`
  - 文字：`#0B1020`
  - 禁用：`rgba(129,140,248,0.35)` + `rgba(229,231,235,0.55)`
- 次按钮：
  - 背景：透明
  - 描边：`Dark-Border`
  - 文字：`Dark-Text-Primary`

## 3.2 输入框与选择器

- 背景：`Dark-Bg-Surface`
- 描边：`Dark-Border`
- 聚焦边框：`Dark-Primary`
- 占位：`Dark-Text-Placeholder`
- 错误边框：`Dark-Danger`

## 3.3 卡片

- 背景：`Dark-Bg-Card`
- 描边：卡片描边规范
- 标题：`Dark-Text-Primary`
- 辅助：`Dark-Text-Secondary`

## 3.4 筛选 Chips（类型/维度）

- 默认：
  - 背景：`Dark-Bg-Surface`
  - 文字：`Dark-Text-Secondary`
  - 边框：`Dark-Border`
- 选中：
  - 背景：`Dark-Primary-Soft`
  - 文字：`Dark-Primary-Emphasis`
  - 边框：`Dark-Primary`

## 3.5 弹窗与提示

- 弹窗背景：`Dark-Bg-Overlay`
- 标题文字：`Dark-Text-Primary`
- 描述文字：`Dark-Text-Secondary`
- 危险操作按钮使用 `Dark-Danger`

---

## 4. 核心页面暗色规范

## 4.1 登录与隐私授权页（Dark）

### 视觉要点
- 页面背景使用 `Dark-Bg-Page`
- 中央说明卡使用 `Dark-Bg-Card` + 轻描边
- 品牌标题使用 `Dark-Primary-Emphasis`
- 协议链接使用 `Dark-Info`

### AI 提示词
```text
请设计微信小程序暗黑模式登录引导弹窗，风格克制高级。该弹窗不是首屏强制页，而是在用户触发新增/编辑/删除/查看我的数据时按需拉起。弹窗包含隐私说明、协议勾选区和主按钮“微信一键登录”。未勾选协议时按钮禁用，要求对比清晰、留白充足、适配 iPhone 390x844。
```

---

## 4.2 新增/编辑记录页（Dark）

### 视觉要点
- 表单卡片使用深色分层（页面背景 < 卡片背景 < 输入区）
- 类型胶囊选中态使用主色浅底
- 备注字数计数使用次级文字色
- 危险操作“删除”按钮使用弱红背景 + 红色文字

### AI 提示词
```text
请设计微信小程序暗黑模式的记录表单页，包含日期、时间（可选）、事件类型3选1（生理现象/亲密互动/自我舒缓）、备注输入（200字限制）。新增态默认显示当前时间，编辑态必须回显该记录已保存时间。页面采用卡片式分层，输入框深色背景，选中态使用蓝紫高亮，底部固定保存按钮，编辑态包含删除按钮和二次确认弹窗风格。
```

---

## 4.3 历史记录列表页（Dark）

### 视觉要点
- 列表项卡片轻描边，避免与背景粘连
- 类型标签采用固定类型色（蓝/绿/紫）
- 时间信息使用次文本色，保证主信息优先级
- 空态插图建议低饱和单色插画

### AI 提示词
```text
请设计微信小程序暗黑模式历史记录页，包含顶部标题与新增按钮、类型筛选栏、卡片列表。每条记录显示类型标签、日期时间、备注摘要，并支持编辑与删除入口。请同时输出空态界面，整体风格专业、低饱和、信息层级明确。
```

---

## 4.4 统计分析页（Dark）

### 视觉要点
- 图表背景保持透明或深色卡片，不使用纯黑网格线
- 坐标轴与网格线使用低对比中性灰
- 重要数值（指标卡数字）使用高亮主色或高亮白
- 保持图表间距，避免暗色下视觉拥挤

### 图表规范
- 柱状图主序列：`Dark-Primary` 渐变
- 饼图三类颜色：
  - 生理现象：`Chart-Physio`
  - 亲密互动：`Chart-Intimacy`
  - 自我舒缓：`Chart-Relief`
- 折线图：主色线 + 20% 透明面积填充

### 柱状图数据与坐标定义（必须严格遵守）
- 横坐标：日期/月份（时间维度），禁止使用类型名称
  - 周视图：周一到周日 7 个日期（MM-DD）
  - 月视图：01~月末
  - 年视图：01~12 月
- 纵坐标：次数（count，聚合值）
- 聚合规则：同一时间桶内记录条数求和后绘制柱子
- 示例：`04-01:2, 04-02:0, 04-03:3, 04-04:1`

### AI 提示词
```text
请设计微信小程序暗黑模式统计分析页，包含周/月/年切换、类型筛选（全部/生理现象/亲密互动/自我舒缓）、柱状图、饼图、折线图以及5个指标卡。柱状图必须满足“横坐标=日期或月份，纵坐标=聚合次数count”，禁止使用类型作为横坐标。要求低饱和深色背景、图表可读性强、卡片分层明显，并提供空态、加载态和错误态。
```

---

## 4.5 我的页（Dark，新增）

### 视觉要点
- 纳入底部 Tab：记录 / 统计 / 我的
- 未登录态展示登录引导卡片，强调“登录后可使用记录与个人统计”
- 已登录态展示用户信息、退出登录按钮与设置入口

### AI 提示词
```text
请设计微信小程序暗黑模式“我的”页面，作为底部Tab之一。页面支持未登录/已登录双状态：未登录时展示登录引导卡片，已登录时展示用户信息与退出按钮。列表包含隐私政策、用户协议、主题设置、版本信息入口，风格与统计页一致。
```

---

## 5. 状态页（Dark）

## 5.1 加载态
- 使用深色骨架屏：`rgba(148,163,184,0.12)` -> `rgba(148,163,184,0.22)` 呼吸动画

## 5.2 空态
- 主文案：`Dark-Text-Primary`
- 辅助文案：`Dark-Text-Secondary`
- 引导按钮：`Dark-Primary`

## 5.3 错误态
- 错误图标：`Dark-Danger`
- 文案：简短可执行（例如“加载失败，请重试”）
- 提供“重试”按钮

## 5.4 会话失效态
- 弹窗提示“登录已过期，请重新登录”
- 主操作按钮“重新登录”使用 `Dark-Primary`

---

## 6. 深浅主题切换策略

- 跟随系统优先（`auto`）
- 用户可手动切换（`light` / `dark`）
- 用户选择持久化到本地缓存
- 切换时仅替换 token，不改变布局结构

建议主题变量命名：
- `--bg-page`
- `--bg-card`
- `--text-primary`
- `--text-secondary`
- `--color-primary`
- `--color-success`
- `--color-danger`

---

## 7. 暗黑模式验收清单

- [ ] 页面主文字对比度达标，阅读无压力
- [ ] 图表在暗色背景下无“糊成一片”现象
- [ ] 表单聚焦、错误、禁用状态区分明显
- [ ] 危险操作视觉强调清晰但不过分刺眼
- [ ] 空态/加载态/错误态均有暗色版本
- [ ] 深浅主题切换后布局无跳动、无错色
- [ ] 与浅色主题语义一致（同一状态同一语义）

---

## 8. 一键生成提示（整包）

```text
你是一名高级移动端UI设计师，请基于以下暗黑模式规范生成微信小程序高保真原型（iPhone 390x844）：
1）登录与隐私授权页
2）新增/编辑记录页
3）历史记录列表页
4）统计分析页（周/月/年+图表+指标卡）
风格要求：私密、克制、现代、低饱和；深色分层明确；图表可读性高；包含空态、加载态、错误态。
颜色参考：
背景 #0B1020、卡片 #131C31、主色 #818CF8、主文字 #E5E7EB、次文字 #9CA3AF、边框 #263248。
类型图表色：
生理现象 #60A5FA，亲密互动 #34D399，自我舒缓 #C084FC。
```
