# ManLog v1 开发任务清单

> 目标：基于 `product.md` 完成 v1 可上线版本（微信小程序）。
> 范围：登录、记录、编辑删除、历史查询、周月年统计、图表展示、云端加密与权限校验。

> **进度说明（2026-04-12 更新）**：下列勾选表示**当前仓库已落地**；未勾选为待办。前端默认 **uni-app + 本地 mock**（未配置 `VITE_API_BASE`）；配置后走 **`src/services/*` + `VITE_API_BASE`**（登录、记录 CRUD、统计 overview）。后端 **`server/`** 已实现 JWT、`/auth/wx-login`（开发态 code→哈希 openid）、记录与统计接口、`sql/schema.sql`。**部署环境**需自备 MySQL 并完成微信 code2session 替换后做端到端验收。**401**：清 token + Toast + `reLaunch` 至登录页（`utils/auth.relaunchToLoginAfterUnauthorized`，并发请求合并为一次跳转）。**记录列表**已支持 `onReachBottom` 分页（API：`page`/`pageSize` + 类型筛选；mock：本地切片）。**小程序端图表与 H5 差异**仍为后续迭代。**敏感字段**已由服务端 **AES-256-GCM +每用户 HKDF 密钥**落库（详见 `design.md` §5.2、`server/src/lib/recordCrypto.js`）；生产密钥建议走 **KMS → 环境变量**。

> **UI 冻结说明（v1 · 联调前）**：**Tab + 记录 / 统计分析 / 我的 / 登录** 各页布局、主流程交互与字段形态（日期、可选时间、三类型、备注 ≤200）已定稿，以 `product.md`、`design.md` 为准。进入接口联调后，前端仅做**小修正**（样式与间距、文案、空态/错误提示、与后端字段对齐），**不**在无评审情况下做大改版或新增同级主页面。以下工作**不视为破坏冻结**，按里程碑单独排期：**小程序端图表方案**、**深色主题**、隐私政策等独立详情页（若与提审要求新增）、**生产微信 code2session**、**主密钥轮换脚本 /全量 KMS 托管**。

## 1. 前端任务（微信小程序）

### 1.1 项目基础搭建
- [x] 初始化小程序工程结构（`pages` / `components` / `utils` / `services`）。
- [x] 配置全局路由、页面权限守卫（匿名可浏览，关键操作拦截登录）。
- [x] 配置环境变量（开发/测试/生产 API 地址；`VITE_API_BASE` + 根目录 `.env.example`）。
- [x] 接入统一请求层（`services/http.js`：Bearer、业务错误、`code !== 0`、401 清会话并跳转登录）。

### 1.2 登录与会话
- [x] 实现微信登录流程（获取 code -> `POST /auth/wx-login`；生产需替换为平台 code2session）。
- [x] 登录态本地存储（token、`userId`；`utils/auth.js`）。
- [x] 演示用本地登录标记（`uni.storage`，非 token；mock 模式沿用）。
- [x] 实现会话失效处理（401 / `code === 40101`：清 token + `reLaunch` 登录页；并发只跳转一次）。
- [x] 增加登录引导页（含隐私政策、用户协议入口）。
- [x] 增加按需登录弹窗（记录页新增/编辑前拉起；协议勾选后模拟登录并继续操作）。

### 1.3 记录功能
- [x] 新增记录页：日期、时间（可空）、事件类型、备注（可空 <= 200）。
- [x] 时间为空时，前端显示“默认当前时间”提示。
- [x] 表单校验（必填项、备注长度、时间格式）。
- [x] 提交成功后刷新历史与统计缓存（本地存储 + eventChannel 通知列表；统计页 onShow 重载）。

### 1.4 历史记录与管理
- [x] 历史记录列表页（按时间倒序展示）。
- [x] 列表分页加载（触底 `onReachBottom`，API `page`/`pageSize`/`eventType`；mock 本地排序后切片）。
- [x] 类型筛选（全部/生理现象/亲密互动/自我舒缓）。
- [x] 编辑记录页（复用新增表单，支持回填与保存）。
- [x] 删除记录功能（弹窗二次确认）。

### 1.5 统计报表页
- [x] 统计维度切换（周/月/年/总）。
- [x] 类型筛选联动统计（默认全部；mock 按类型权重缩放，接接口后替换为真实聚合）。
- [x] 频次柱状图组件。
- [x] 分类占比饼图组件。
- [x] 频率趋势折线图组件。
- [x] 指标卡组件（周均、月均、最长间隔、最短间隔、高频时间段）。
- [x] 空态与无数据态 UI（记录列表筛选无结果、统计无数据时图表区与指标卡占位）。

### 1.6 通用体验与质量
- [x] 统一 Loading、错误提示、重试机制（`utils/ui`：`showLoading`/`toast`/`runWithLoading`；重试机制待接接口后强化）。
- [x] 页面埋点（登录成功、记录保存/删除、统计浏览与筛选；`utils/track` 占位，可接正式上报）。
- [ ] 关键交互的可用性自测（3 步内完成记录）。

### 1.7 我的页与导航
- [x] 底部 Tab 调整为“记录 / 统计 / 我的”。
- [x] 实现我的页未登录态（登录引导）与已登录态（账号信息、退出登录）。
- [x] 实现我的页菜单：隐私政策、用户协议、主题设置、版本信息。

---

## 2. 后端任务（API 与业务服务）

### 2.1 服务基础
- [x] 初始化后端项目结构（`server/src`：`routes` / `services` / `lib` / `db`）。
- [x] 接入配置管理、日志、异常处理（`config.js`、Fastify logger、路由内 try/catch）。
- [x] 接入鉴权中间件（JWT Bearer，`routes/v1/index.js` `preHandler`）。

### 2.2 登录与用户体系
- [x] 实现微信 code 换取用户标识（`openid`）流程（**开发态**哈希占位；上线接微信 API）。
- [x] 建立 `openid -> userId` 映射表（`users`）。
- [x] 登录接口签发会话 token（含过期时间）。
- [x] 实现登出接口（`POST /auth/logout`；JWT 场景以客户端清理为主）。

### 2.3 记录 CRUD
- [x] 新增记录接口（时间可空，服务端补默认当前时间）。
- [x] 查询记录列表接口（分页、类型筛选、时间区间）。
- [x] 查询记录详情接口（用于编辑回填）。
- [x] 更新记录接口（仅允许本人数据）。
- [x] 删除记录接口（软删 `is_deleted`）。

### 2.4 统计与报表 API
- [x] 周统计接口（按周一到周日口径，`GET /stats/overview`）。
- [x] 月统计接口。
- [x] 年统计接口。
- [x] 图表聚合接口（柱状图、饼图、趋势图、指标卡）。
- [x] 类型筛选聚合（默认全部类型，支持单类型过滤）。

### 2.5 安全与合规
- [x] 所有接口接入 `userId` 鉴权与数据隔离（业务路由层）。
- [x] 输入校验与防注入（参数校验、备注长度等；ORM 参数化查询）。
- [x] 敏感字段加密存储（`event_type_enc` / `note_enc` + `event_type_lookup`；见 `server/sql/schema.sql` 与 `recordCrypto.js`）。
- [ ] 审计日志（新增/编辑/删除记录行为）。

---

## 3. 数据任务（模型、迁移、统计口径）

### 3.1 数据库与表结构
- [x] 创建用户表（最小字段集，包含 `openid` 映射）。
- [x] 创建事件记录表（日期、时间、类型、备注、时间戳等）。
- [x] 创建必要索引（`user_id + event_datetime`、`user_id + event_date` 等，见 `server/sql/schema.sql`）。
- [x] 确定删除策略字段（`is_deleted`）。

### 3.2 加密与密钥
- [x] 设计字段加密方案（应用层 AES-256-GCM；生产主密钥建议云 KMS 注入 `MANLOG_RECORD_MASTER_KEY`）。
- [x] 明确加密/解密触发时机（写入前加密、读取后解密；`recordService` 全路径）。
- [ ] 输出密钥轮转预留策略（v1 可仅预留接口）。

### 3.3 统计口径实现
- [x] 定义自然周边界（周一 00:00:00 至周日 23:59:59）。
- [x] 定义时间分桶（00-06/06-12/12-18/18-24）。
- [x] 定义最长/最短间隔计算规则（按相邻事件时间差）。
- [x] 定义周均/月均计算公式（统计周期总次数 / 周数或月数）。
- [x] 统一时区处理策略（服务端 `config.tz` + `dayjs`）。

### 3.4 数据治理
- [x] 备注长度约束（<= 200 字）与编码规范（UTF-8）。
- [ ] 数据回填与修复脚本模板（预留）。
- [ ] 备份策略与恢复演练方案（按环境定义）。

---

## 4. 测试任务（功能、接口、性能、安全）

### 4.1 功能测试
- [ ] 登录流程：首次登录、会话过期、重新登录。
- [ ] 记录流程：新增、编辑、删除、空时间默认值、备注边界值。
- [ ] 列表流程：分页、筛选、排序、空态。
- [ ] 报表流程：周/月/年切换、类型筛选联动、图表渲染正确性。

### 4.2 接口测试
- [ ] 鉴权测试（未登录、token 过期、伪造 userId）。
- [ ] 参数校验测试（缺参数、非法类型、超长备注）。
- [ ] 统计接口口径测试（边界日期、跨月跨年、闰年）。

### 4.3 安全测试
- [ ] 越权访问测试（访问他人记录）。
- [ ] 注入与恶意输入测试（脚本、SQL 关键字、异常字符）。
- [ ] 敏感数据落库检查（部署后抽检密文列 / 密钥权限；实现已完成）。

### 4.4 性能测试
- [ ] 记录列表分页性能（1k/5k/10k 数据量）。
- [ ] 报表接口响应时间（周/月/年聚合）。
- [ ] 峰值并发下稳定性检查（基础压测）。

### 4.5 回归与上线验收
- [ ] 回归清单维护（每次发布执行）。
- [ ] 灰度验证与监控（错误率、接口耗时）。
- [ ] 小程序提审前检查（隐私协议、功能完整、无阻塞问题）。

---

## 5. 里程碑建议

### M1：基础可用版本
- 登录、记录新增、历史列表、基础 CRUD 完成。

### M2：统计报表版本
- 周月年统计、三类图表、指标卡完成。

### M3：安全与提审版本
- 加密与权限强化、测试通过、提审材料齐备。

---

## 6. 交付物清单
- [x] 前端代码与页面说明（uni-app 静态流程 + mock；正式说明文档可随 v1 收口补全）
- [ ] 后端 API 文档
- [x] 数据库建表与迁移脚本（`server/sql/schema.sql`）
- [ ] 测试报告（功能/接口/安全/性能）
- [ ] 小程序提审材料（隐私政策、用户协议、功能说明）

---

## 7. 第一周冲刺计划（UI 前置，按天）

> 目标：先一次性完成全部页面 UI 与交互，再进行接口联调与回归。

### Day 1（UI 基础搭建日）
- [x] 小程序初始化工程结构（页面目录、组件目录、样式规范）。
- [x] 完成全局路由与页面框架（登录页、记录页、历史页、统计页）。
- [x] 完成设计 token（颜色、字号、间距、按钮、表单样式）。
- [x] 完成通用组件骨架（筛选栏、页面内空态、按需登录弹窗；顶部栏为各页自定义；抽象公共 Empty 组件可选）。

### Day 2（记录域页面 UI 一次完成）
- [x] 完成新增记录页 UI（日期/时间/类型/备注表单）。
- [x] 完成历史记录列表页 UI（列表项、筛选栏）。
- [x] 列表分页或底部分页占位 UI。
- [x] 完成编辑记录页 UI（复用新增页并提供回填态）。
- [x] 完成删除二次确认弹窗与交互动效。
- [x] 统一术语文案：生理现象/亲密互动/自我舒缓（替换旧术语）。

### Day 3（统计域页面 UI 一次完成）
- [x] 完成统计主页 UI（周/月/年/总切换、类型筛选 chips）。
- [x] 接入柱状图、饼图、折线图的前端展示组件（先用 mock 数据）。
- [x] 完成指标卡 UI（周均、月均、间隔、高频时段）。
- [x] 完成统计页面空态、无数据态设计。
- [x] 完成统计页面加载态（首次进入骨架闪烁；再次进入不重复遮挡）。

### Day 4（UI 收口与交互验收）
- [x] 全量页面走查（视觉一致性、响应式、交互可达性）。
- [x] 完成 UI 缺陷修复（样式、文案、提示、边界状态）。
- [x] 完成前端 mock 数据联动，确保页面“可演示”。
- [x] 冻结 UI 结构（字段、组件、状态位），进入联调阶段（范围见文首「UI 冻结说明」）。

### Day 5（后端核心接口实现）
- [x] 完成登录相关接口（`POST /auth/wx-login`、`POST /auth/logout`）。
- [x] 完成记录 CRUD 接口（新增、列表、详情、更新、删除）。
- [x] 完成服务端 `userId` 权限隔离与参数校验。
- [ ] 输出接口文档与示例响应供前端联调。

### Day 6（统计接口实现 + 前后端联调）
- [x] 完成 `GET /stats/overview`（week/month/year + 类型筛选）。
- [x] 完成统计聚合口径（周一到周日、时间分桶、间隔计算）。
- [x] 前端替换 mock 数据为真实接口，打通登录与记录主链路（`VITE_API_BASE` 开关）。
- [ ] 修复联调问题，完成核心页面可用性验证（**待部署/有 DB 环境回归**）。

### Day 7（全链路回归与冲刺验收）
- [ ] 执行全链路回归（登录 -> 新增 -> 编辑 -> 删除 -> 统计）。
- [ ] 执行异常场景测试（401、参数错误、空数据、越权）。
- [ ] 输出第一周验收结果与遗留问题清单。
- [ ] 评审第二周重点（加密增强、性能优化、提审准备）。

---

## 8. 接口联调顺序（建议）

> 原则（UI 前置版）：先冻结 UI 与交互，再按“鉴权 -> CRUD -> 统计”顺序联调。

### 阶段 0：UI 冻结
1. 登录页、记录页、历史页、统计页 UI 全部完成
2. 页面状态齐全（加载、空态、异常态）
3. 统一字段命名与前端数据模型（与接口字段对齐）

联调前通过标准：
- [x] 所有页面可在 mock 数据下正常演示。
- [x] UI 结构与交互不再做大改，只做小修正（与文首「UI 冻结说明」一致）。

### 阶段 A：鉴权先行
1. `POST /auth/wx-login`
2. `POST /auth/logout`

联调通过标准：
- [x] 登录成功返回 token 与 userId。
- [x] 前端可持久化 token 并附带到后续请求。
- [x] token 失效后可正确跳转登录（`reLaunch` `/pages/auth/login`）。

### 阶段 B：记录主链路
3. `POST /records`
4. `GET /records`
5. `GET /records/{id}`
6. `PUT /records/{id}`
7. `DELETE /records/{id}`

联调通过标准：
- [x] 新增后列表可见（代码已接通；**待环境验收**）。
- [x] 编辑后详情与列表一致（同上）。
- [x] 删除后列表不再显示（软删生效；同上）。
- [x] 无法访问/操作他人数据（服务端按 `userId` 隔离；同上）。

### 阶段 C：统计与图表
8. `GET /stats/overview?dimension=week`
9. `GET /stats/overview?dimension=month`
10. `GET /stats/overview?dimension=year`

联调通过标准：
- [x] 三个维度返回结构一致（**待环境验收**）。
- [x] 类型筛选与默认“全部类型”一致可用（同上）。
- [x] 柱状图、饼图、折线图、指标卡均可渲染（H5；小程序图表单独排期）。
- [x] 周口径为周一到周日，边界日期正确（服务端实现；同上）。

### 阶段 D：回归与稳定性
11. 全链路回归（登录 -> 新增 -> 编辑 -> 删除 -> 统计）
12. 异常场景回归（401、参数错误、空数据、极限备注长度）

---

## 9. 联调风险与阻塞预案

- [ ] **风险：微信登录能力未就绪** -> 预案：开发阶段提供测试登录开关，仅限 dev 环境。
- [ ] **风险：统计口径前后端理解不一致** -> 预案：以 `design.md` 口径为唯一标准，并补充样例数据。
- [ ] **风险：图表库适配问题** -> 预案：先以表格/简版图占位，接口先稳定。
- [ ] **风险：加密引入性能波动** -> 预案：先覆盖敏感字段最小集，压测后再扩展。
