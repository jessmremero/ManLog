# 男性生理与行为记录小程序 - 产品需求文档

## 1. 项目概述

### 1.1 背景
本项目是一款面向男性用户的微信小程序，用于记录个人生理与亲密行为相关事件，并通过可视化统计帮助用户进行长期自我观察与分析。

### 1.2 目标与价值
- 提供低门槛、快速、私密的行为记录方式。
- 基于记录数据生成周/月/年统计报表，帮助用户理解频率变化与周期规律。
- 支持类型筛选与多图表展示，便于从不同维度解读数据。

### 1.3 目标用户与使用场景
- 目标用户：有自我健康管理意识的男性用户。
- 典型场景：用户在事件发生后即时记录，周期性查看统计趋势，复盘近期变化。

---

## 2. 核心功能

### 2.1 用户登录与账户体系（微信）
- 采用“可浏览 + 关键操作前登录”策略：首次进入无需强制登录，可先浏览产品说明与示例内容。
- 后端基于微信登录态建立稳定业务 `userId`，并与用户数据绑定。
- 未登录状态下，可访问浏览型页面（示例统计/功能介绍/隐私协议），不可读写个人记录数据。
- 当用户触发新增、编辑、删除、查看个人真实统计等关键操作时，再弹出登录引导。
- 会话过期时需自动校验并引导重新登录，保证数据访问安全。

登录触发清单（未登录时）：
- 点击“新增记录”
- 点击“编辑记录”
- 点击“删除记录”
- 进入“我的真实历史记录”页
- 点击“查看我的统计”或访问个人真实统计数据

### 2.1.1 导航结构（信息架构）
- 底部 Tab 建议包含：记录、统计、我的。
- “我的”页承载：登录状态、隐私政策、用户协议、主题设置、版本信息等。
- 未登录时“我的”页提供清晰的登录入口与能力说明。

### 2.2 事件类型定义
当前版本支持 3 类事件：
- 生理现象
- 亲密互动
- 自我舒缓

说明：
- 面向用户展示与筛选统一使用“生理现象 / 亲密互动 / 自我舒缓”。
- 若后端需要技术枚举，可内部映射为 `EMISSION` / `SEX` / `MASTURBATION`，前端不直接展示枚举名。

### 2.3 事件记录功能
- 记录粒度：按实际发生事件记录，发生一次即新增一条记录。
- 每条记录字段：
  - 日期（必填）
  - 时间（非必填；未填写时默认使用当前记录时间）
  - 事件类型（必填，三选一）
  - 备注（非必填，最多 200 字）
- 时间字段交互规则：
  - 新增记录：时间默认显示“当前时间”（用户可手动修改）。
  - 编辑记录：时间回显该条记录已保存时间（可手动修改）。
- 统计展示按聚合结果呈现，例如某天“自我舒缓”3 次（由该天记录条数聚合得到，而非手工输入次数）。

### 2.4 记录管理功能
- 支持查看历史记录列表（按时间倒序）。
- 支持编辑记录（日期、时间、类型、备注可修改）。
- 支持删除记录（需二次确认，防误删）。
- 支持按类型筛选查看历史明细。

### 2.5 统计与报表功能
统计维度：
- 周
- 月
- 年
- 总（累计）

时间口径：
- 自然周定义为 **周一到周日**。

筛选规则：
- 支持按类型筛选。
- 默认“全部类型”，数值为三类事件累计值。

图表与指标：
1. 频次柱状图  
   - 纵轴：次数（该时间桶内记录条数，必须是聚合值而非单条值）
   - 横轴：时间（按当前统计维度切换）
     - 周视图：周一至周日 7 个日期桶（如 04-01, 04-02 ... 04-07）
     - 月视图：当月每日日期桶（01~28/29/30/31）
     - 年视图：1~12 月份桶
   - 柱子含义：每根柱子代表对应日期/月份下的总次数
2. 分类占比图（饼图）  
   - 展示“生理现象 / 亲密互动 / 自我舒缓”三类占比
3. 频率趋势图（折线图）  
   - 观察连续周期频次变化趋势（与柱状图不同的时间粒度）
   - 横轴（按当前维度）：
     - 周视图：最近 8 周（Wxx）
     - 月视图：最近 12 个月（YYYY-MM）
     - 年视图：最近 5 年（YYYY）
     - 总视图：按年（项目启动年至今）
   - 纵轴：每个周期的聚合次数
4. 平均统计卡片  
   - 周均次数  
   - 月均次数  
   - 最长间隔  
   - 最短间隔  
   - 高频时间段

高频时间段分桶：
- 00:00-05:59
- 06:00-11:59
- 12:00-17:59
- 18:00-23:59

指标卡展示规则：
- 高频时间段必须展示“时间段文案”（例如 `18:00-23:59`），不展示单一时间点（例如 `21:00`）。
- 周均次数 / 月均次数按当前维度显示：
  - 周视图：周均=本周总次数；月均显示 `-`
  - 月视图：周均=当月总次数/当月周数；月均=当月总次数
  - 年视图：周均=当年总次数/当年周数；月均=当年总次数/12
  - 总视图：周均=累计总次数/累计周数；月均=累计总次数/累计月数

---

## 3. 数据模型（产品层）

### 3.1 记录实体字段
- `id`
- `userId`
- `eventType`（EMISSION/SEX/MASTURBATION，前端显示为生理现象/亲密互动/自我舒缓）
- `eventDate`（日期）
- `eventTime`（时间，可为空）
- `eventDateTime`（用于排序与统计的最终时间戳）
- `note`（备注，可为空，<=200 字）
- `createdAt`
- `updatedAt`

### 3.2 统计口径
- 单次事件 = 1 条记录。
- 任意周期内“次数”=记录条数。
- 日统计值 = 当日记录条数。
- 类型筛选后统计值仅包含筛选范围内记录。
- 默认“全部类型”时为全部事件总和。

### 3.3 时间处理规则
- 用户未填写时间时，系统自动写入当前记录时间。
- 统计按用户本地时区计算自然日/自然周/自然月/自然年边界。

---

## 4. 非功能需求

### 4.1 隐私与安全
- 数据存储在云端。
- 传输过程采用 HTTPS/TLS 加密。
- 云端敏感数据加密存储（至少覆盖行为类型与备注等敏感字段）。
- 用户数据强隔离：每个用户只能访问自己的数据。
- 不收集与核心业务无关的个人敏感信息。

### 4.2 权限与访问控制
- 所有读写接口均要求合法登录态。
- 所有查询、新增、编辑、删除操作均按 `userId` 做服务端权限校验。
- 禁止通过前端参数越权访问其他用户数据。

### 4.3 合规与审核要求（微信小程序）
- 提供隐私政策与用户协议入口。
- 登录与数据使用遵循最小必要授权原则。
- 明确告知用户数据用途仅用于个人记录与统计分析。

### 4.4 性能与容量
- 常规历史数据查询与报表切换应保持流畅。
- 支持用户长期累积数据（按年）后仍可稳定统计与展示。

### 4.5 易用性
- 记录操作路径简短，支持快速完成一次记录。
- 图表页默认展示“全部类型 + 当前周期”，降低理解成本。

---

## 5. 版本范围

### 5.1 当前版本（MVP）
- 微信登录与会话管理
- 三类事件记录
- 历史记录查看、筛选、编辑、删除
- 周/月/年统计
- 频次柱状图、分类占比图、趋势折线图、统计卡片
- 云端存储与基础加密能力
- 微信小程序上线所需基础能力

### 5.2 暂不纳入（明确不做）
- 社交功能
- 医疗诊断建议
- 复杂健康指标联动（睡眠/饮食/运动）
- 多平台形态扩展（独立 App / Web 后台）
- 高级 AI 诊断结论
