# ManLog UI 风格规范与原型生成稿（v1）

> 用途：将本文件直接提供给 Figma AI、Galileo、Uizard、Visily 等工具，快速生成高保真页面。  
> 设计目标：私密、克制、现代、可读性高；适配微信小程序。

## 1. 设计风格定位

- 关键词：`Private` `Calm` `Clinical` `Minimal`
- 视觉气质：低饱和、低噪声、强调秩序感与安全感
- 情绪表达：避免刺激性颜色，突出“记录”与“自我观察”的中性语义

---

## 2. 设计系统（Design Tokens）

## 2.1 颜色（建议）

- 主色 `Primary`: `#4F46E5`（稳重蓝紫）
- 主色浅 `Primary-Soft`: `#EEF0FF`
- 强调色 `Accent`: `#0EA5A4`（用于积极状态提示）
- 成功 `Success`: `#16A34A`
- 警示 `Warning`: `#D97706`
- 危险 `Danger`: `#DC2626`

- 文本主色 `Text-Primary`: `#111827`
- 文本次色 `Text-Secondary`: `#6B7280`
- 占位文本 `Text-Placeholder`: `#9CA3AF`

- 背景 `Bg-Page`: `#F8FAFC`
- 卡片背景 `Bg-Card`: `#FFFFFF`
- 分割线 `Border`: `#E5E7EB`

## 2.2 字体与字号

- 字体：系统默认无衬线（微信小程序优先系统字体）
- 标题：`20 / 28 / Semibold`
- 二级标题：`16 / 24 / Medium`
- 正文：`14 / 22 / Regular`
- 辅助文案：`12 / 18 / Regular`
- 数字指标：`24 / 32 / Semibold`

## 2.3 圆角与阴影

- 小圆角：`8`
- 中圆角：`12`
- 大圆角：`16`
- 卡片阴影：`0 4 12 rgba(17,24,39,0.06)`
- 弹窗阴影：`0 8 24 rgba(17,24,39,0.12)`

## 2.4 间距系统（8pt）

- `4 / 8 / 12 / 16 / 24 / 32`
- 页面左右边距：`16`
- 卡片内边距：`16`
- 组件垂直间距：`12`

## 2.5 组件规范（关键）

- 按钮高度：`44`
- 输入框高度：`44`
- 筛选器高度：`36`
- 列表项最小高度：`56`
- 图表卡片高度建议：`220 ~ 280`

---

## 3. 页面信息架构（v1）

- 底部 Tab：记录 / 统计 / 我的
- 页面 1：登录与隐私授权弹窗（按需拉起，不强制首屏）
- 页面 2：新增/编辑记录页
- 页面 3：历史记录列表页
- 页面 4：统计分析页（周/月/年）
- 页面 5：我的页（登录状态、协议入口、主题设置、版本信息）

---

## 4. 原型页面详细文案（可直接生成）

## 4.1 页面 1：登录与隐私授权页

### 页面目标
完成微信登录与隐私授权确认；默认不强制首屏登录，在触发关键操作时按需拉起。

### 布局结构
1. 顶部区域：品牌名 `ManLog` + 副标题“私密记录，科学观察”
2. 中部说明卡片：
   - 文案 1：仅用于个人记录与统计
   - 文案 2：数据云端加密存储
   - 文案 3：你可随时管理自己的记录
3. 协议区：
   - 勾选框“我已阅读并同意《用户协议》《隐私政策》”
4. 底部主按钮：
   - `微信一键登录`

### 交互状态
- 未勾选协议：主按钮置灰
- 登录中：按钮 loading
- 登录失败：顶部 toast 提示，可重试
- 登录成功：关闭弹窗并回到触发前页面，继续原操作流程

### 生成提示词（可粘贴到 AI）
```text
设计一个微信小程序登录引导弹窗，风格克制高级，主色为蓝紫色。页面包含品牌标题、3条隐私说明、协议勾选区、主按钮“微信一键登录”。未勾选协议时按钮禁用。不要“先看看”或跳过按钮。该弹窗在用户触发新增/编辑/删除/查看我的统计时按需拉起。整体采用卡片式布局，留白充足，适配 iPhone 390x844。
```

---

## 4.2 页面 2：新增/编辑记录页

### 页面目标
快速完成一次记录，支持新增和编辑复用同一页面。

### 表单字段
- 日期（必填，日期选择器）
- 时间（非必填，时间选择器；空则默认当前时间）
- 事件类型（必填，单选 chips：生理现象 / 亲密互动 / 自我舒缓）
- 备注（非必填，多行输入，200 字限制）

### 布局结构
1. 顶部导航：标题（新增记录 / 编辑记录）
2. 表单卡片：
   - 日期行
   - 时间行（右侧提示“可不填，默认当前时间”）
   - 类型选择行（三个可切换胶囊按钮）
   - 备注输入区（显示字数计数）
3. 底部固定操作区：
   - 主按钮：`保存`
   - 次按钮（编辑态显示）：`删除`

### 交互状态
- 新增态时间：页面打开时时间控件默认填入“当前时间”（可修改）。
- 编辑态时间：时间控件必须回显该条记录已保存时间（可修改）。
- 校验失败：字段下方红字提示
- 保存成功：toast + 返回列表
- 删除操作：二次确认弹窗

### 生成提示词
```text
设计一个微信小程序表单页，用于记录私密事件。字段有日期、时间（可选）、事件类型（生理现象/亲密互动/自我舒缓三选一）、备注（最多200字）。新增态默认显示当前时间；编辑态必须回显该记录已保存时间。底部固定“保存”按钮，编辑态额外有“删除”按钮。界面简洁、可读性高、卡片式布局，提供错误提示和字数计数视觉。
```

---

## 4.3 页面 3：历史记录列表页

### 页面目标
快速浏览历史记录，支持筛选、编辑入口、删除操作。

### 布局结构
1. 顶部区域：
   - 页面标题：`历史记录`
   - 右上角按钮：`新增`
2. 筛选栏：
   - 类型筛选（全部/生理现象/亲密互动/自我舒缓）
3. 列表区（按时间倒序）：
   - 每项展示：类型标签、日期时间、备注摘要
   - 每项操作：点击进入编辑；左滑或更多菜单可删除
4. 分页加载：
   - 底部 `加载更多`

### 列表项样式建议
- 左侧：类型色条（不同类型微差色）
- 中部：主信息（类型+时间）
- 下方：备注摘要（无备注显示“无备注”）
- 右侧：箭头或更多按钮

### 空态文案
- 标题：`还没有记录`
- 说明：`点击右上角“新增”，开始第一次记录`
- 按钮：`去新增`

### 生成提示词
```text
设计一个微信小程序历史列表页，包含顶部标题和新增按钮、类型筛选栏、卡片列表。列表项显示类型标签、日期时间、备注摘要，支持进入编辑和删除操作。请同时设计空态页面，整体风格简洁且专业，适配移动端。
```

---

## 4.4 页面 4：统计分析页（核心）

### 页面目标
按周/月/年查看频次变化与结构占比，支持类型筛选。

### 布局结构
1. 顶部标题：`统计分析`
2. 控制区（两行）：
   - 第一行：维度切换（周/月/年/总）
   - 第二行：类型筛选（全部/生理现象/亲密互动/自我舒缓）
3. 图表卡片区：
   - 卡片 A：频次柱状图
   - 卡片 B：分类占比饼图
   - 卡片 C：频率趋势折线图
4. 指标卡区（2x3 网格）：
   - 周均次数
   - 月均次数
   - 最长间隔
   - 最短间隔
   - 高频时间段
5. 辅助区：
   - 数据更新时间
   - 口径说明入口（i 按钮）

### 图表视觉建议
- 柱状图：主色渐变（浅到深）
- 饼图：三类型固定色系（统一映射）
- 折线图：主色线 + 轻量面积填充

### 柱状图数据与坐标定义（必须严格遵守）
- 图名：类型频次分布（按当前筛选与周期）
- 横坐标：日期/月份标签（时间维度）
  - 周视图：周一到周日的 7 个日期（MM-DD）
  - 月视图：当月 01~月末日期
  - 年视图：01~12 月
  - 总视图：按年份（YYYY）
- 纵坐标：次数（count）
- 聚合规则：同一时间桶内的记录条数求和后再绘制柱子
- 禁止行为：不得把类型名称放到横坐标；不得使用单条记录值直接绘制
- 示例（周视图）：
  - 04-01: 2
  - 04-02: 0
  - 04-03: 3
  - 04-04: 1

### 频率趋势图数据与坐标定义（必须严格遵守）
- 周视图：横轴=最近 8 周（Wxx），纵轴=每周总次数
- 月视图：横轴=最近 12 个月（YYYY-MM），纵轴=每月总次数
- 年视图：横轴=最近 5 年（YYYY），纵轴=每年总次数
- 总视图：横轴=全部年份（YYYY），纵轴=每年总次数
- 禁止行为：不得与柱状图使用完全相同横轴口径（例如周视图不能再用具体日期）

### 图形美观约束（柱状图）
- 单柱最大视觉宽度建议 `28px`
- 组间最小间距建议 `8px`
- 当横轴桶较少时，柱体居中但不拉伸超过最大宽度

### 空态/异常态
- 无数据：展示引导“先记录几次，再看趋势”
- 加载中：骨架屏卡片
- 接口失败：重试按钮
- 匿名态：可展示示例统计或空态引导；点击“查看我的统计”时拉起登录
- 未登录触发登录操作清单：新增、编辑、删除、查看我的历史、查看我的统计

### 指标卡展示规则（必须严格遵守）
- 周均次数 / 月均次数：
  - 周视图：周均=本周总次数；月均显示 `-`
  - 月视图：周均=当月总次数/当月周数；月均=当月总次数
  - 年视图：周均=当年总次数/当年周数；月均=当年总次数/12
  - 总视图：周均=累计总次数/累计周数；月均=累计总次数/累计月数
- 高频时间段必须显示时间段（如 `18:00-23:59`），不能显示时间点（如 `21:00`）

### 生成提示词
```text
设计一个微信小程序统计分析页，包含周/月/年切换、类型筛选（全部/生理现象/亲密互动/自我舒缓）、三张图表（柱状图/饼图/折线图）和5个统计指标卡。柱状图必须是“横坐标=日期或月份，纵坐标=聚合后的次数count”，禁止用类型作为横坐标。要求专业克制、低饱和、卡片化布局，突出数据可读性，并包含无数据态和加载态。
```

---

## 5. 全局状态页（建议一并生成）

- 加载态：骨架屏（列表骨架、图表骨架、卡片骨架）
- 错误态：插画 + 文案 + 重试按钮
- 无网络态：提示条 + 重连操作
- 权限失效态：弹窗提示并引导重新登录
- 匿名访问受限态：用户点击新增/编辑/删除/查看我的数据时，弹出登录引导层

---

## 5.1 我的页（新增）

### 页面目标
提供个人入口与设置中心，在未登录与已登录状态下都可访问。

### 布局结构
1. 顶部用户卡片
   - 未登录：头像占位 + 文案“登录后可记录与查看个人统计” + 登录按钮
   - 已登录：头像/昵称（可脱敏） + 退出登录
2. 功能列表
   - 隐私政策
   - 用户协议
   - 主题设置（浅色/深色/跟随系统）
   - 版本信息

### 生成提示词
```text
设计微信小程序“我的”页面，支持未登录与已登录双状态。未登录显示登录引导卡片，已登录显示用户信息与退出按钮。页面包含隐私政策、用户协议、主题设置、版本信息入口，风格与记录页/统计页保持一致。
```

---

## 6. AI 生成工作流（推荐）

1. 使用本文件生成 4 个页面首稿（每页至少 2 个视觉方案）。
2. 选择一个主风格，统一颜色/按钮/输入框/卡片规范。
3. 为每页补齐 3 个状态：加载、空态、错误。
4. 在 Figma 建立组件库（Button/Input/Chip/Card/StatCard）。
5. 锁定 v1 视觉规范后再进入前后端联调。

---

## 7. 视觉验收清单（你可直接打勾）

- [ ] 四个核心页面风格一致
- [ ] 主色与类型色映射统一
- [ ] 字体层级清晰（标题/正文/辅助）
- [ ] 点击区域满足移动端可触达（>= 44）
- [ ] 空态、加载态、错误态都已覆盖
- [ ] 表单校验提示清楚可见
- [ ] 数据图表在深浅背景下均可读
- [ ] 整体观感符合“私密、克制、专业”
